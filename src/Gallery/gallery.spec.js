import React from "react";
import { shallow } from "enzyme";

/* Sample of a component test
describe("Gallery", () => {
  it("Item should render properly", () => {
    const component = shallow(<SomeComponent someProp={'someValue'} />);
    expect(component).toMatchSnapshot();
  });
}); */
